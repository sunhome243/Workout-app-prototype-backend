version: "3.12"

services:
  db:
    image: postgres:14-alpine
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=itismepassword
      - POSTGRES_DB=prototype
    ports:
      - "5433:5432" 
    volumes:
      - db_data:/var/lib/postgresql/data

  user-service:
    build: ./user-service
    ports:
      - "8020:8020"
    environment:
      - DATABASE_URL=postgresql://postgres:itismepassword@db:5432/prototype

  workout-service:
    build: ./workout-service
    ports:
      - "8010:8010"
    environment:
      - DATABASE_URL=postgresql://postgres:itismepassword@db:5432/prototype

  ai-service:
    build: ./ai-service
    ports:
      - "8040:8040"
    environment:
      - DATABASE_URL=postgresql://postgres:itismepassword@db:5432/prototype  

  homework-service:
    build: ./homework-service
    ports:
      - "8003:8003"
    environment:
      - DATABASE_URL=postgresql://postgres:itismepassword@db:5432/prototype 

volumes:
  db_data: 